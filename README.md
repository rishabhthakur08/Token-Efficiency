# Token Efficiency

This project is a numerical machine-learning exercise for predicting output-token usage from prompt, model, and subject features.

## Data Note

The active token counts under `data/token_counts/` are reproducible synthetic learning targets designed to contain realistic, noisy relationships for regression practice. They are not measurements of the listed models.

The original measured token counts are preserved under `data/original_output_tokens/`.
